# Task #N — {任务标题}

## 变更说明
> 本节给用户（PM）阅读，不含实现细节。

**类型**：新功能 / Bug修复 / 优化 / 重构（选其一）

{1-2句话，用户视角：这个任务增加了什么功能、修复了什么问题、优化了什么体验。}

---

## 目标

{1-3句话说明本任务要解决什么问题，以及完成后用户/系统可观测的变化。不写实现细节。}

## 必读契约

- `docs/api/{layer}.md` # `{module}` 节（重点：{关键知识点}）
- （按需列出，至少 1 项）

## 改动范围

- **修改**：`{path/to/file.py}`
- **新建**：`{path/to/newfile.py}`（若有）
- **更新**：`docs/api/{layer}.md`（若改动了公开签名）
- **不许碰**：{明确列出禁止改动的文件}

## 接口约定

{描述新增/修改的公开函数或组件的行为契约，格式：}

`function_name(param: Type, ...) -> ReturnType`
- 行为：做什么（不是怎么做）
- 副作用：写库 / 改 session_state / 触发 rerun 等
- 约束：调用前提、禁止场景

{Bug 修复类任务写：现象 + 预期行为 + 涉及文件范围，根因和具体修复由实现工自行诊断。}

> 不写伪代码或实现细节——实现工读现有代码后自行决定 How。
> 只在跨模块架构约束时才给额外提示。

## 不要做

- {明确列出禁止的操作，例如：不加 try/except、不改函数签名、不引入新依赖}

## 验收清单

- [ ] `{smoke test 命令}` 通过
- [ ] `{start command}` 启动无报错
- [ ] 手工流程跑通：{具体用户可见路径}
- [ ] `docs/api/{layer}.md` 已同步（若有签名变更）
- [ ] commit 信息符合规范（建议 `{type}({scope}): {描述} · 关联 #N`）
- [ ] 在 worktree 分支提交，未 push main

## 架构师备注

{任务背景、设计决策依据、已知风险、与其他任务的依赖关系。
若实现中发现契约遗漏或矛盾，立即停手写 BLOCKED: <冲突点>，不自行扩大改动范围。}
